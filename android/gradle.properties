# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE are ignored when building from command line, check
# build.gradle files instead.

# When configured, Gradle will run in offline mode and will not download any dependencies from the internet.
# Offline mode is disabled by default and useless when further internet connectivity is required.
# When running Gradle from the command line, use flag --offline to force the offline mode.
# Offline mode may cause unexpected errors and exceptions without internet connectivity.

android.useAndroidX=true
android.enableJetifier=true
